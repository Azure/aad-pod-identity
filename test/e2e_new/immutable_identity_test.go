// +build e2e_new

package e2e_new

import (
	. "github.com/onsi/ginkgo"
)

var _ = Describe("When deploying immutable identity", func() {
	It("should not delete the Immutable Identity from vmss when the deployment is deleted", func() {

	})
})
